name: bitcoin_node
description: Bitcoin Core RPC adapter — implements remote data source interfaces
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  rpc_client:
    path: ../rpc_client
  shared_kernel:
    path: ../shared_kernel
  transaction:
    path: ../transaction
  wallet:
    path: ../wallet

