name: data
description: Bitcoin wallet data layer — repository and service implementations
publish_to: none

environment:
  sdk: ^3.11.3

resolution: workspace

dependencies:
  crypto: 3.0.7
  domain:
    path: ../domain
  flutter:
    sdk: flutter
  pointycastle: 4.0.0
  rpc_client:
    path: ../rpc_client
  storage:
    path: ../storage
  uuid: 4.5.3

dev_dependencies:
  test: 1.30.0
