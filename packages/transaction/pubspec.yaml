name: transaction
description: Transaction and UTXO domain module
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  shared_kernel:
    path: ../shared_kernel
  wallet:
    path: ../wallet

dev_dependencies:
  test: ^1.30.0
  
  keys:
    path: ../keys
  rpc_client:
    path: ../rpc_client
