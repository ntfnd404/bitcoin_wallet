name: transaction
description: Transaction and UTXO domain module
publish_to: none

environment:
  sdk: ^3.11.3

resolution: workspace

dependencies:
  address:
    path: ../address
  shared_kernel:
    path: ../shared_kernel

dev_dependencies:
  test: 1.30.0
